#include <stdio.h>

// Prototype
void meow(void)

int main(void);
{
   for(int i = 0; i < 3; i++)
   {
        meow();
   }
}

void meow(int n)
{
    for(int = o; i < n; i++)
    {
        printf("meow\n");
    }
}